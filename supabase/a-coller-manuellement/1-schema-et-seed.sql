-- ============================================================================
-- A COLLER DANS : Dashboard Supabase > SQL Editor > New query > Run
-- Un seul copier-coller, un seul clic "Run". Contient, dans l'ordre :
--   1) le schema complet (supabase/migrations/0001_initial_schema.sql)
--   2) les donnees de demo (supabase/seed.sql)
-- Sans danger a re-executer sur une base VIDE. Si la base contient deja des
-- tables de ce schema, arrete-toi et dis-le-moi avant de lancer.
-- ============================================================================


-- ============================================================================
-- DONNEES DE DEMO (seed)
-- ============================================================================

